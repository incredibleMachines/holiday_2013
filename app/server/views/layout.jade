!!!
html
	head
		title= title
		link(rel='stylesheet', href='/vendor/bootstrap.min.css')
		meta(name='viewport', content='width=device-width, initial-scale=1.0')
		link(rel='stylesheet', href='/vendor/bootstrap-responsive.css')
		link(rel='stylesheet', href='/vendor/bootstrap-modal.css')
		- if(typeof(holiday)!=='undefined')
				link(rel='stylesheet', href='/css/holiday.css')
				//link(rel='stylesheet', href='/css/history.css')
				script(type='text/javascript', src='/vendor/swfobject.js')
				script(type='text/javascript')
					// For version detection, set to min. required Flash Player version, or 0 (or 0.0.0), for no version detection. 
					var swfVersionStr = "11.1.0";
					// To use express install, set to playerProductInstall.swf, otherwise the empty string. 
					var xiSwfUrlStr = "/swf/playerProductInstall.swf";
					var flashvars = {};
					var params = {};
					params.quality = "high";
					params.bgcolor = "#ffffff";
					params.allowscriptaccess = "sameDomain";
					params.allowfullscreen = "true";
					var attributes = {};
					attributes.id = "holiday_tree";
					attributes.name = "holiday_tree";
					attributes.align = "middle";
					swfobject.embedSWF(
					"/swf/holiday_tree.swf", "flashContent", 
					"100%", "100%", 
					swfVersionStr, xiSwfUrlStr, 
					flashvars, params, attributes);
					// JavaScript enabled so display the flashContent div in case it is not replaced with a swf object.
					swfobject.createCSS("#flashContent", "display:block;text-align:left;");
					console.log('yea dog')
		- else
				link(rel='stylesheet',href='/css/style.css')
		
		link(rel='stylesheet', href='/vendor/farbtastic.css')
		link(rel="stylesheet", href="http://code.jquery.com/ui/1.10.3/themes/smoothness/jquery-ui.css")
		script(type='text/javascript')
			(function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
			(i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
			m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
			})(window,document,'script','//www.google-analytics.com/analytics.js','ga');
			
			ga('create', 'UA-45421450-2', 'treeeee.com');
			ga('send', 'pageview');
	body!= body
		